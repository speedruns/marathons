{% extends "layouts/main.html.j2" %}

{% block main %}
  {% if redirect %}
    <form action="/login?redirect={{redirect}}" method="POST">
  {% else %}
    <form action="/login" method="POST">
  {% endif %}
    <div class="field is-horizontal">
      <div class="field-label">
      </div>

      <div class="field-body">
        <p>
          Don't have an account?
          <a href="/profile/new">Create one now!</a>
          If you created an account for
          <a href="https://fifteenfortyfive.org" target="_blank" rel="noopener">The 1545</a>,
          you can use that account to log in here.
        </p>
      </div>
    </div>

    {% include "sessions/_form.html.j2" %}

    <div class="field is-horizontal">
      <div class="field-label">
      </div>
      <div class="field-body">
        <div class="field">
          <div class="control is-expanded">
            <input class="button is-primary" type="submit" value="Submit">
          </div>
        </div>
      </div>
    </div>
  </form>
{% endblock %}
